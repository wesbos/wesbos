import React from 'react';

import Instagram from '../Instagram';

export default {
  title: 'components/Instagram',
  component: Instagram,
};

const Component = () => <Instagram />;

export const Default = Component.bind({});
